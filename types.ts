const today = new Date();
today.getMonth();

const person = {
	//if we hover person it gives us what its properties types are, but no methods.
	age: 30,
};

class Color {}

const red = new Color(); //if we hover over red, it tells us which class it is
